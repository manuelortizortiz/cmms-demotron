async function loadTable(endpoint){const r=await fetch(endpoint);const data=await r.json();const el=document.getElementById('genericTable');if(!data.length){el.innerHTML='<p>No hay datos cargados en esta tabla.</p>';return}const cols=Object.keys(data[0]);el.innerHTML=`<table><thead><tr>${cols.map(c=>`<th>${c}</th>`).join('')}</tr></thead><tbody>${data.map(row=>`<tr>${cols.map(c=>`<td>${row[c]??''}</td>`).join('')}</tr>`).join('')}</tbody></table>`}
